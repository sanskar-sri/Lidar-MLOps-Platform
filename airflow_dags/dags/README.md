# DAG Source

Put every Airflow DAG that should be controlled by the Mac `data-platform`
controller in this folder.

Copy or sync this folder to the Windows Airflow Docker project's mounted DAG
directory, usually `/opt/airflow/dags` inside the container.

